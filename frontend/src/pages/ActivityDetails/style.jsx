import styled from "styled-components";

export default styled.main`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 81vh;
  width: 100vw;
  background-color: var(--color-bg-primary);

  @media screen and (min-width: 700px) {
    // adapt for larger screens, if necessary
  }
`;
